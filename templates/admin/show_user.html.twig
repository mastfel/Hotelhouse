{% extends "base.html.twig" %}

{% block title %}Gestion des membres - HouseHotel
{% endblock %}

{% block body %}

	{% if user is not defined %}
		<h1 class="text-center my-4">Gestion des membres</h1>
	{% else %}
		<h1 class="text-center my-4">Modifier le membre</h1>
	{% endif %}

	<div class="row">
		<div class="col-11 mx-auto my-4">

			 {# {{ form_start(form) }} #}
			 <div class="row"> 
							 {# <div class="col-4">
								{{ form_row(form.roles) }}
							</div> #}
{# 			
							{{ form_row(form.created_at | date('d/m/Y') ) }}
			
							{{ form_row(form.updatedAt | date('d/m/Y') ) }} 
							 #}
							</div>
							
			{# {{ form_end(form) }} #}

		</div>
	</div>
{% endblock %}
